{
  "name": "Iamnotyourfan",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:console": "tsc -b && vite build --mode console",
    "make:console-bundle": "node scripts/make-console-bundle.js",
    "preview": "vite preview",
    "dev:landing": "vite --mode landing"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^24.12.3",
    "esbuild": "^0.28.0",
    "sass": "^1.100.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
